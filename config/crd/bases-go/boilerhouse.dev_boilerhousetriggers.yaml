---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: boilerhousetriggers.boilerhouse.dev
spec:
  group: boilerhouse.dev
  names:
    kind: BoilerhouseTrigger
    listKind: BoilerhouseTriggerList
    plural: boilerhousetriggers
    shortNames:
    - bht
    singular: boilerhousetrigger
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.type
      name: Type
      type: string
    - jsonPath: .spec.workloadRef
      name: Workload
      type: string
    - jsonPath: .status.phase
      name: Phase
      type: string
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: BoilerhouseTrigger is the Schema for the boilerhousetriggers
          API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: BoilerhouseTriggerSpec defines the desired state of a BoilerhouseTrigger.
            properties:
              config:
                description: Config is a free-form map of trigger configuration.
                type: object
                x-kubernetes-preserve-unknown-fields: true
              driver:
                description: Driver is the driver to use for the trigger.
                type: string
              driverOptions:
                description: DriverOptions is a free-form map of driver options.
                type: object
                x-kubernetes-preserve-unknown-fields: true
              guards:
                description: Guards is a list of guard conditions.
                items:
                  description: TriggerGuard defines a guard condition for a trigger.
                  properties:
                    config:
                      description: Config is the guard configuration.
                      type: object
                      x-kubernetes-preserve-unknown-fields: true
                    type:
                      description: Type is the type of guard.
                      type: string
                  type: object
                type: array
              tenant:
                description: Tenant defines how the tenant is determined.
                properties:
                  from:
                    description: From specifies the source field for the tenant identifier.
                    type: string
                  prefix:
                    description: Prefix is a prefix applied to the tenant identifier.
                    type: string
                  static:
                    description: Static sets a fixed tenant identifier for all events.
                    type: string
                type: object
              type:
                description: Type is the trigger type.
                enum:
                - webhook
                - slack
                - telegram
                - cron
                type: string
              workloadRef:
                description: WorkloadRef is the name of the BoilerhouseWorkload this
                  trigger targets.
                type: string
            required:
            - type
            - workloadRef
            type: object
          status:
            description: BoilerhouseTriggerStatus defines the observed state of a
              BoilerhouseTrigger.
            properties:
              detail:
                description: Detail provides additional information about the current
                  phase.
                type: string
              phase:
                description: Phase is the current phase of the trigger.
                enum:
                - Active
                - Error
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
