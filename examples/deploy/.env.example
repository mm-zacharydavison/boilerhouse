# Boilerhouse Production Configuration
# Copy this file to .env and configure as needed

# Docker Connection
# -----------------
# In production with docker-compose, DOCKER_HOST is set automatically to
# connect through docker-socket-proxy (tcp://docker-proxy:2375).
# You don't need to set it here.
#
# For local development outside of docker-compose:
# Linux (default socket):
# DOCKER_HOST=unix:///var/run/docker.sock
#
# macOS with Docker Desktop:
# DOCKER_HOST=unix:///Users/your-username/.docker/run/docker.sock

# Pool Configuration
# ------------------
# Number of pre-warmed containers to maintain in each pool
BOILERHOUSE_POOL_SIZE=5

# Maximum containers per node (across all pools)
BOILERHOUSE_MAX_CONTAINERS=50

# Container idle timeout in milliseconds (default: 5 minutes)
# BOILERHOUSE_IDLE_TIMEOUT_MS=300000

# S3 State Sync Configuration (optional)
# --------------------------------------
# Uncomment and configure if using state sync to S3
# AWS_ACCESS_KEY_ID=your-access-key
# AWS_SECRET_ACCESS_KEY=your-secret-key
# AWS_REGION=us-east-1

# For MinIO (local S3-compatible storage)
# AWS_ACCESS_KEY_ID=minioadmin
# AWS_SECRET_ACCESS_KEY=minioadmin
# AWS_ENDPOINT_URL=http://minio:9000
