# Architecture

## System Overview

<!-- WRITE: diagram or description of major components — API, runtimes, DB, storage, triggers -->

## Monorepo Structure

<!-- WRITE: packages/, apps/, workloads/ layout and what lives where -->

## Request Lifecycle

<!-- WRITE: what happens when a tenant claims an instance end-to-end -->

## Instance State Machine

<!-- WRITE: states (starting, active, hibernating, hibernated, restoring, destroying, destroyed) and transitions -->

## Data Flow

<!-- WRITE: how workload definitions become running containers — parsing, scheduling, runtime dispatch -->
