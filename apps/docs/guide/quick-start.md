# Quick Start

## Prerequisites

<!-- WRITE: bun, docker, node version requirements -->

## Install

<!-- WRITE: clone, bun install, build steps -->

## Run the API

<!-- WRITE: env vars needed, `bun run dev` or binary invocation -->

## Register a Workload

<!-- WRITE: example workload file, POST /api/v1/workloads or CLI command -->

## Claim an Instance

<!-- WRITE: POST /api/v1/tenants/:id/claim, what you get back -->

## Next Steps

<!-- WRITE: links to workloads, configuration, deployment guides -->
