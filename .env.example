# Boilerhouse Development Configuration
# Copy this file to .env and configure as needed

# Docker Connection
# -----------------
# Linux (default socket):
# DOCKER_HOST=unix:///var/run/docker.sock

# macOS with Docker Desktop:
# DOCKER_HOST=unix://${HOME}/.docker/run/docker.sock

# Remote Docker daemon (e.g., docker-socket-proxy in production):
# DOCKER_HOST=tcp://localhost:2375

# Pool Configuration
# ------------------
# BOILERHOUSE_POOL_SIZE=5
# BOILERHOUSE_MAX_CONTAINERS=50
# BOILERHOUSE_IDLE_TIMEOUT_MS=300000

# API Server
# ----------
# BOILERHOUSE_API_PORT=3000
# BOILERHOUSE_API_HOST=0.0.0.0

# Directories
# -----------
# BOILERHOUSE_STATE_DIR=/var/lib/boilerhouse/states
# BOILERHOUSE_SECRETS_DIR=/var/lib/boilerhouse/secrets
# BOILERHOUSE_SOCKET_DIR=/var/run/boilerhouse

# Workloads directory - where YAML workload specs are loaded from
# For development (path relative to apps/api/ since that's where dev script runs):
BOILERHOUSE_WORKLOADS_DIR=../../workloads
# For production (set via docker-compose):
# BOILERHOUSE_WORKLOADS_DIR=/etc/boilerhouse/workloads

# Database
# --------
# BOILERHOUSE_DB_PATH=./data/boilerhouse.db
